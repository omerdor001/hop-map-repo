# Root conftest.py — anchors pytest's rootdir to the repository root.
# Having this file ensures pytest treats agent/tests/ and server/tests/
# as distinct subpackages rather than colliding on the shared name "tests".
