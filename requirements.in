# HopMap — root abstract dependency constraints (server + agent combined)
#
# THIS FILE IS HUMAN-MAINTAINED.  Edit it to add, remove, or change deps.
# The pinned lockfile (requirements.txt) is generated from this file — never
# edit requirements.txt by hand.
#
# Regenerate the lockfile:
#   pip-compile requirements.in
#
# To install only one component use its own requirements file:
#   pip install -r server/requirements.txt
#   pip install -r agent/requirements.txt
#
# Also requires Tesseract OCR (agent only):
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki

# ── Shared ──────────────────────────────────────────────────────────────────
pydantic-settings>=2.2.0
requests>=2.31.0

# ── Server ──────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
pymongo>=4.6.0
ollama>=0.1.7
openpyxl>=3.1.0
pyahocorasick>=1.4.0

# ── Agent (Windows only) ─────────────────────────────────────────────────────
pywin32>=306
psutil>=5.9.0
mss>=9.0.0
pytesseract>=0.3.13
Pillow>=10.0.0
pyperclip>=1.9.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
mongomock>=4.1.0
