# HopMap Server — abstract dependency constraints
#
# THIS FILE IS HUMAN-MAINTAINED.  Edit it to add, remove, or change deps.
# The pinned lockfile (requirements.txt) is generated from this file — never
# edit requirements.txt by hand.
#
# Regenerate the lockfile:
#   pip-compile requirements.in
#
# NOTE: For production Linux deployments, regenerate on Linux (or use the
# --platform / --python-version flags) to get platform-correct pins:
#   pip-compile requirements.in --platform linux --python-version 3.11
#
# Install pinned deps:
#   pip install -r requirements.txt

fastapi>=0.110.0
uvicorn>=0.27.0
wsproto>=1.2.0
pymongo>=4.6.0
pydantic-settings>=2.2.0
ollama>=0.1.7
openpyxl>=3.1.0
pyahocorasick>=1.4.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
slowapi>=0.1.9

# Testing — kept in the same file; use pip-compile --extra=dev for separation
# if the project grows to warrant it.
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
mongomock>=4.1.0
requests>=2.32.0
email-validator>=2.0.0
